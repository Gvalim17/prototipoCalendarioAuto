# RF03, RF07, RF08 — Implementação Completa

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implementar completamente RF03 (reutilização de disciplinas via autocomplete), RF07/RF08 (fluxo interativo de resolução de conflitos em 2 fases), e gerar ERS v1.6.

**Architecture:** Backend ganha `suggested_date` em cada conflito retornado + endpoint `POST /schedules/resolve-conflicts/` + endpoint `GET /disciplines/search`. Frontend ganha step 2b (resolução de conflitos) entre geração e calendário, e autocomplete no formulário de disciplinas.

**Tech Stack:** Python 3.13 + FastAPI + SQLAlchemy (backend), React + TypeScript + Tailwind + Axios (frontend), python-docx (ERS).

---

## Mapa de Arquivos

| Arquivo | Mudança |
|---|---|
| `backend/app/schemas/base_schemas.py` | Adicionar `suggested_date` em `SkippedDate`; novos schemas `ConflictResolution`, `ResolveConflictsRequest`, `ResolvedScheduleResponse` |
| `backend/app/services/schedule_generator.py` | Computar `suggested_date` em cada conflito; novo método `apply_resolutions` |
| `backend/app/main.py` | Novos endpoints: `GET /disciplines/search`, `POST /schedules/resolve-conflicts/` |
| `frontend/src/pages/ScheduleForm.tsx` | Step 2b de resolução interativa; step 2 vira step 3 |
| `frontend/src/pages/MBAList.tsx` | Autocomplete de disciplinas com `GET /disciplines/search` |

---

## Task 1: Backend — Atualizar Schemas

**Files:**
- Modify: `backend/app/schemas/base_schemas.py`

- [ ] **Step 1: Adicionar `suggested_date` em `SkippedDate` e novos schemas**

Localizar o bloco que começa na linha 105 de `backend/app/schemas/base_schemas.py`:

```python
class SkippedDate(BaseModel):
    date: date
    reason: str
```

Substituir por:

```python
class SkippedDate(BaseModel):
    date: date
    reason: str
    suggested_date: Optional[date] = None

class ConflictResolution(BaseModel):
    original_date: date
    action: str  # "auto" | "manual"
    resolved_date: date

class ResolveConflictsRequest(BaseModel):
    config: ScheduleConfigBase
    resolutions: List[ConflictResolution]

class ResolvedScheduleResponse(BaseModel):
    dates: List[date]
    config: ScheduleConfigBase
```

- [ ] **Step 2: Verificar que imports em `base_schemas.py` já têm `Optional` e `List`**

Linha 3 já tem: `from typing import List, Optional` — nenhuma mudança necessária.

- [ ] **Step 3: Adicionar imports novos em `main.py`**

Abrir `backend/app/main.py`, linha 8–17. Adicionar `ResolveConflictsRequest, ResolvedScheduleResponse, ConflictResolution` no bloco de imports de schemas:

```python
from .schemas.base_schemas import (
    MBACreate, MBARead, 
    ModuleCreate, ModuleRead,
    DisciplineCreate, DisciplineRead,
    HolidayCreate, HolidayRead, 
    RecessCreate, RecessRead,
    ScheduleConfigBase, ScheduleResponse,
    MBAUpdate, ModuleUpdate, DisciplineUpdate,
    FullScheduleCreate, FullScheduleRead, ScheduledClassCreate,
    CalendarEventRead, PreviewExportRequest,
    ResolveConflictsRequest, ResolvedScheduleResponse
)
```

- [ ] **Step 4: Commit**

```bash
git add backend/app/schemas/base_schemas.py backend/app/main.py
git commit -m "feat(rf07): add SkippedDate.suggested_date and conflict resolution schemas"
```

---

## Task 2: Backend — suggested_date no Gerador

**Files:**
- Modify: `backend/app/services/schedule_generator.py`

- [ ] **Step 1: Atualizar `generate_schedule` para computar `suggested_date`**

Em `backend/app/services/schedule_generator.py`, substituir o método `generate_schedule` completo pelo código abaixo. A única mudança é que `skipped_dates` agora inclui `suggested_date` (próxima data válida no mesmo dia da semana, procurada a partir de `current_date + increment_days`):

```python
from datetime import date, timedelta
from typing import List, Optional
from sqlalchemy.orm import Session
from ..models.base import Holiday, Recess, ScheduleConfig, RecurrenceType

class ScheduleGeneratorService:
    @staticmethod
    def is_blocked(check_date: date, holidays: dict, recesses: List[tuple]) -> bool:
        if check_date in holidays:
            return True
        for r_start, r_end, _ in recesses:
            if r_start <= check_date <= r_end:
                return True
        return False

    @classmethod
    def find_next_valid(cls, from_date: date, holidays: dict, recesses: List[tuple], max_search: int = 60) -> Optional[date]:
        """Encontra a próxima data válida (não bloqueada) a partir de from_date."""
        candidate = from_date
        for _ in range(max_search):
            if not cls.is_blocked(candidate, holidays, recesses):
                return candidate
            candidate += timedelta(days=1)
        return None

    @classmethod
    def generate_schedule(cls, db: Session, config: ScheduleConfig) -> dict:
        holidays = {h.date: h.description for h in db.query(Holiday).all()}
        recesses = [(r.start_date, r.end_date, r.description) for r in db.query(Recess).all()]
        
        generated_dates = []
        skipped_dates = []
        current_date = config.start_date
        
        if config.recurrence == RecurrenceType.NA:
            is_holiday_desc = holidays.get(current_date)
            recess_desc = next((desc for s, e, desc in recesses if s <= current_date <= e), None)
            
            if is_holiday_desc:
                suggested = cls.find_next_valid(current_date + timedelta(days=1), holidays, recesses)
                skipped_dates.append({
                    "date": current_date,
                    "reason": f"Feriado: {is_holiday_desc}",
                    "suggested_date": suggested
                })
                return {"dates": [], "skipped": skipped_dates}
            if recess_desc:
                suggested = cls.find_next_valid(current_date + timedelta(days=1), holidays, recesses)
                skipped_dates.append({
                    "date": current_date,
                    "reason": f"Recesso: {recess_desc}",
                    "suggested_date": suggested
                })
                return {"dates": [], "skipped": skipped_dates}
            
            return {"dates": [current_date], "skipped": []}

        increment_days = 7 if config.recurrence == RecurrenceType.SEMANAL else 14
        classes_count = 0
        max_attempts = 1000
        attempts = 0

        diff = (config.day_of_week - current_date.weekday() + 7) % 7
        current_date += timedelta(days=diff)

        while classes_count < config.num_classes and attempts < max_attempts:
            attempts += 1
            
            is_holiday_desc = holidays.get(current_date)
            recess_desc = next((desc for s, e, desc in recesses if s <= current_date <= e), None)

            if is_holiday_desc or recess_desc:
                reason = f"Feriado: {is_holiday_desc}" if is_holiday_desc else f"Recesso: {recess_desc}"
                # suggested_date: próxima ocorrência válida no mesmo dia da semana
                next_occurrence = current_date + timedelta(days=increment_days)
                suggested = cls.find_next_valid(next_occurrence, holidays, recesses)
                skipped_dates.append({
                    "date": current_date,
                    "reason": reason,
                    "suggested_date": suggested
                })
                current_date += timedelta(days=increment_days)
                continue
            
            generated_dates.append(current_date)
            classes_count += 1
            current_date += timedelta(days=increment_days)

        return {"dates": generated_dates, "skipped": skipped_dates}
```

- [ ] **Step 2: Verificar que o backend sobe sem erros**

```bash
cd /Users/guilhermevalim/Developer/qualiquanti/prototipoCalendarioAuto/backend
.venv/bin/python -c "from app.services.schedule_generator import ScheduleGeneratorService; print('ok')"
```

Saída esperada: `ok`

- [ ] **Step 3: Commit**

```bash
git add backend/app/services/schedule_generator.py
git commit -m "feat(rf07): compute suggested_date for each skipped conflict"
```

---

## Task 3: Backend — Novos Endpoints

**Files:**
- Modify: `backend/app/main.py`

- [ ] **Step 1: Adicionar endpoint `GET /disciplines/search`**

Após a linha do `@app.delete("/disciplines/{discipline_id}")` (linha ~155 de `main.py`), inserir:

```python
@app.get("/disciplines/search", response_model=List[dict])
def search_disciplines(q: str = "", db: Session = Depends(get_db)):
    if len(q) < 2:
        return []
    results = (
        db.query(Discipline, Module, MBA)
        .join(Module, Discipline.module_id == Module.id)
        .join(MBA, Module.mba_id == MBA.id)
        .filter(Discipline.name.ilike(f"%{q}%"))
        .limit(10)
        .all()
    )
    return [
        {
            "id": disc.id,
            "name": disc.name,
            "code": disc.code,
            "module_name": mod.name,
            "mba_name": mba.name,
        }
        for disc, mod, mba in results
    ]
```

- [ ] **Step 2: Adicionar endpoint `POST /schedules/resolve-conflicts/`**

Após o endpoint `POST /generate-schedule/` (após linha ~296 de `main.py`), inserir:

```python
@app.post("/schedules/resolve-conflicts/", response_model=ResolvedScheduleResponse)
def resolve_conflicts(data: ResolveConflictsRequest, db: Session = Depends(get_db)):
    try:
        cfg_model = ScheduleConfig(**data.config.model_dump())
        result = ScheduleGeneratorService.generate_schedule(db, cfg_model)
        
        # Substituir datas dos conflitos resolvidos
        resolved_map = {str(r.original_date): r.resolved_date for r in data.resolutions}
        base_dates = list(result["dates"])
        
        for resolution in data.resolutions:
            base_dates.append(resolution.resolved_date)
        
        final_dates = sorted(set(base_dates))
        
        return ResolvedScheduleResponse(dates=final_dates, config=data.config)
    except Exception as e:
        raise HTTPException(status_code=500, detail=str(e))
```

- [ ] **Step 3: Testar os dois endpoints via curl**

```bash
# Teste search (precisa do backend rodando)
curl "http://localhost:8000/disciplines/search?q=fin" | python3 -m json.tool
```

Saída esperada: lista de disciplinas contendo "fin" no nome (ou lista vazia se não houver).

- [ ] **Step 4: Commit**

```bash
git add backend/app/main.py
git commit -m "feat(rf03,rf08): add /disciplines/search and /schedules/resolve-conflicts endpoints"
```

---

## Task 4: Frontend — Step 2b Resolução de Conflitos (ScheduleForm.tsx)

**Files:**
- Modify: `frontend/src/pages/ScheduleForm.tsx`

O fluxo muda de `step 1 → 2` para `step 1 → 2 (conflitos) → 3 (calendário)`.
Quando `skipped.length === 0`, vai direto do step 1 para o step 3.

- [ ] **Step 1: Adicionar estado de resoluções no início do componente**

Logo após a linha 29 (`const [pendingOverride...`), adicionar:

```typescript
// RF-07/RF-08: interactive conflict resolution
const [resolutions, setResolutions] = useState<Record<string, { action: 'auto' | 'manual'; resolved_date: string }>>({});
```

- [ ] **Step 2: Alterar `handleGenerate` para ir para step 2 quando há conflitos, step 3 quando não há**

Localizar o bloco que muda step (linha ~109):
```typescript
setStep(2);
```

Substituir por:
```typescript
if (res.data.skipped.length > 0) {
    // inicializa resoluções com sugestão automática para cada conflito
    const initial: Record<string, { action: 'auto' | 'manual'; resolved_date: string }> = {};
    for (const s of res.data.skipped) {
        if (s.suggested_date) {
            initial[s.date] = { action: 'auto', resolved_date: s.suggested_date };
        }
    }
    setResolutions(initial);
    setStep(2);
} else {
    setStep(3);
}
```

- [ ] **Step 3: Adicionar função `handleApplyResolutions`**

Após a função `handleGenerate`, adicionar:

```typescript
const handleApplyResolutions = async () => {
    const startDateObj = new Date(formData.start_date + 'T00:00:00');
    const jsDay = startDateObj.getDay();
    const pyDay = jsDay === 0 ? 6 : jsDay - 1;

    setLoading(true);
    try {
        const resolutionList = generatedResult.skipped.map((s: any) => {
            const r = resolutions[s.date];
            return {
                original_date: s.date,
                action: r?.action ?? 'auto',
                resolved_date: r?.resolved_date ?? s.suggested_date ?? s.date,
            };
        });

        const res = await axios.post(`${API_BASE}/schedules/resolve-conflicts/`, {
            config: { ...formData, day_of_week: pyDay },
            resolutions: resolutionList,
        });
        setGeneratedResult({ dates: res.data.dates, skipped: [] });
        if (res.data.dates.length > 0) {
            setCurrentMonth(new Date(res.data.dates[0] + 'T00:00:00'));
        }
        setStep(3);
    } catch (err: any) {
        alert('Erro ao aplicar resoluções: ' + (err.response?.data?.detail || err.message));
    } finally {
        setLoading(false);
    }
};
```

- [ ] **Step 4: Atualizar `handleFinalSave` para usar `step === 3` no guard (se existir)**

Verificar se há algum `step === 2` no botão de salvar e trocar para `step === 3`. Buscar por `step === 2` no JSX e trocar para `step === 3`.

- [ ] **Step 5: Adicionar bloco JSX do Step 2 (Resolução de Conflitos)**

Localizar no JSX o bloco `{step === 2 && (` (linha ~530). Antes desse bloco, inserir o novo step 2 de conflitos:

```tsx
{step === 2 && (
    <div className="space-y-6 animate-in fade-in duration-500">
        <div className="flex items-center gap-4">
            <button onClick={() => setStep(1)} className="p-2 rounded-xl bg-slate-800 hover:bg-slate-700 text-slate-400 hover:text-white transition-all">
                <ArrowLeft size={20} />
            </button>
            <div>
                <h3 className="text-2xl font-bold text-white">Resolução de Conflitos</h3>
                <p className="text-slate-400 text-sm mt-1">Escolha como tratar cada data bloqueada antes de confirmar o cronograma.</p>
            </div>
        </div>

        <div className="space-y-4">
            {generatedResult.skipped.map((s: any) => {
                const r = resolutions[s.date];
                return (
                    <div key={s.date} className="glass p-5 rounded-2xl border border-amber-500/20 space-y-4">
                        <div className="flex items-start justify-between">
                            <div>
                                <p className="text-white font-semibold">
                                    {new Date(s.date + 'T00:00:00').toLocaleDateString('pt-BR', { weekday: 'long', day: '2-digit', month: 'long', year: 'numeric' })}
                                </p>
                                <p className="text-amber-400 text-sm flex items-center gap-1 mt-1">
                                    <AlertCircle size={14} />
                                    {s.reason}
                                </p>
                            </div>
                        </div>

                        <div className="grid grid-cols-2 gap-3">
                            <button
                                onClick={() => setResolutions(prev => ({
                                    ...prev,
                                    [s.date]: { action: 'auto', resolved_date: s.suggested_date }
                                }))}
                                className={`p-3 rounded-xl border text-sm font-semibold transition-all ${
                                    r?.action === 'auto'
                                        ? 'bg-green-600/20 border-green-500/50 text-green-400'
                                        : 'bg-slate-800/50 border-slate-700 text-slate-400 hover:border-slate-600'
                                }`}
                            >
                                <CheckCircle2 size={16} className="mx-auto mb-1" />
                                Ajuste Automático
                                {s.suggested_date && (
                                    <p className="text-xs mt-1 font-normal opacity-80">
                                        → {new Date(s.suggested_date + 'T00:00:00').toLocaleDateString('pt-BR')}
                                    </p>
                                )}
                            </button>

                            <div className={`p-3 rounded-xl border text-sm font-semibold transition-all cursor-pointer ${
                                r?.action === 'manual'
                                    ? 'bg-blue-600/20 border-blue-500/50 text-blue-400'
                                    : 'bg-slate-800/50 border-slate-700 text-slate-400 hover:border-slate-600'
                            }`}>
                                <CalendarDays size={16} className="mx-auto mb-1" />
                                Ajuste Manual
                                <input
                                    type="date"
                                    className="mt-2 w-full bg-slate-900 border border-slate-700 rounded-lg px-2 py-1 text-xs text-white focus:outline-none focus:ring-1 focus:ring-blue-500"
                                    value={r?.action === 'manual' ? r.resolved_date : ''}
                                    onChange={(e) => {
                                        if (e.target.value) {
                                            setResolutions(prev => ({
                                                ...prev,
                                                [s.date]: { action: 'manual', resolved_date: e.target.value }
                                            }));
                                        }
                                    }}
                                />
                            </div>
                        </div>
                    </div>
                );
            })}
        </div>

        <button
            onClick={handleApplyResolutions}
            disabled={loading || generatedResult.skipped.some((s: any) => !resolutions[s.date]?.resolved_date)}
            className="w-full bg-blue-600 hover:bg-blue-700 disabled:opacity-50 disabled:cursor-not-allowed text-white font-bold py-4 rounded-2xl transition-all flex items-center justify-center gap-2"
        >
            {loading ? 'Processando...' : 'Aplicar Resoluções e Ver Cronograma'}
            <ChevronRight size={20} />
        </button>
    </div>
)}
```

- [ ] **Step 6: Trocar `{step === 2 &&` (calendário antigo) para `{step === 3 &&`**

O bloco do calendário que começa com `{step === 2 && (` deve ser trocado para `{step === 3 && (`.

Também trocar qualquer referência ao step no indicador de progresso (se existir) de step 2 → 3.

- [ ] **Step 7: Verificar que o fluxo sem conflitos vai direto para step 3**

Confirmar que quando `res.data.skipped.length === 0`, o código vai para `setStep(3)` (adicionado no Step 2 desta task).

- [ ] **Step 8: Commit**

```bash
git add frontend/src/pages/ScheduleForm.tsx
git commit -m "feat(rf07,rf08): add interactive conflict resolution step 2b"
```

---

## Task 5: Frontend — Autocomplete de Disciplinas (MBAList.tsx)

**Files:**
- Modify: `frontend/src/pages/MBAList.tsx`

- [ ] **Step 1: Adicionar estado de autocomplete no componente `MBADetail`**

Localizar a linha 219 de `MBAList.tsx`:
```typescript
const [newDiscipline, setNewDiscipline] = useState({ name: '', code: '' });
```

Adicionar após ela:
```typescript
const [discSearchResults, setDiscSearchResults] = useState<any[]>([]);
const [discSearchTimeout, setDiscSearchTimeout] = useState<ReturnType<typeof setTimeout> | null>(null);
const [selectedExistingDisc, setSelectedExistingDisc] = useState<number | null>(null);
```

- [ ] **Step 2: Adicionar função de busca com debounce**

Após a função `deleteDiscipline` (~linha 308), adicionar:

```typescript
const searchDisciplines = (query: string) => {
    setNewDiscipline(prev => ({ ...prev, name: query }));
    setSelectedExistingDisc(null);
    if (discSearchTimeout) clearTimeout(discSearchTimeout);
    if (query.length < 2) {
        setDiscSearchResults([]);
        return;
    }
    const t = setTimeout(async () => {
        try {
            const res = await axios.get(`${API_URL}/disciplines/search?q=${encodeURIComponent(query)}`);
            setDiscSearchResults(res.data);
        } catch {
            setDiscSearchResults([]);
        }
    }, 300);
    setDiscSearchTimeout(t);
};

const selectExistingDiscipline = (disc: any) => {
    setNewDiscipline({ name: disc.name, code: disc.code });
    setSelectedExistingDisc(disc.id);
    setDiscSearchResults([]);
};
```

- [ ] **Step 3: Atualizar `addDiscipline` para enviar `existing_discipline_id`**

Localizar `addDiscipline` (~linha 279):
```typescript
const addDiscipline = async (moduleId: number) => {
    if (!newDiscipline.name || !newDiscipline.code) return;
    try {
        await axios.post(`${API_URL}/disciplines/`, { ...newDiscipline, module_id: moduleId });
        setNewDiscipline({ name: '', code: '' });
        setSelectedModuleId(null);
        fetchDetails();
    } catch (error) {
        console.error('Error adding discipline:', error);
    }
};
```

Substituir por:
```typescript
const addDiscipline = async (moduleId: number) => {
    if (!newDiscipline.name || !newDiscipline.code) return;
    try {
        const payload: any = { ...newDiscipline, module_id: moduleId };
        if (selectedExistingDisc) payload.existing_discipline_id = selectedExistingDisc;
        await axios.post(`${API_URL}/disciplines/`, payload);
        setNewDiscipline({ name: '', code: '' });
        setSelectedExistingDisc(null);
        setDiscSearchResults([]);
        setSelectedModuleId(null);
        fetchDetails();
    } catch (error) {
        console.error('Error adding discipline:', error);
    }
};
```

- [ ] **Step 4: Substituir o input de Nome pelo autocomplete no JSX**

Localizar (linha ~395–400):
```tsx
<input 
    type="text" placeholder="Nome"
    className="bg-slate-900 border border-slate-800 rounded-lg px-3 py-2 text-white text-sm"
    value={newDiscipline.name}
    onChange={(e) => setNewDiscipline({...newDiscipline, name: e.target.value})}
/>
```

Substituir por:
```tsx
<div className="relative">
    <input 
        type="text" placeholder="Nome (buscar existente ou criar nova)"
        className="w-full bg-slate-900 border border-slate-800 rounded-lg px-3 py-2 text-white text-sm"
        value={newDiscipline.name}
        onChange={(e) => searchDisciplines(e.target.value)}
        autoComplete="off"
    />
    {selectedExistingDisc && (
        <div className="absolute right-2 top-2 text-xs text-green-400 flex items-center gap-1">
            <CheckCircle2 size={12} />
            Reutilizando
        </div>
    )}
    {discSearchResults.length > 0 && (
        <div className="absolute top-full left-0 right-0 z-50 bg-slate-900 border border-slate-700 rounded-xl mt-1 overflow-hidden shadow-xl max-h-48 overflow-y-auto">
            {discSearchResults.map((d: any) => (
                <button
                    key={d.id}
                    type="button"
                    onClick={() => selectExistingDiscipline(d)}
                    className="w-full text-left px-3 py-2 text-sm hover:bg-slate-800 transition-colors"
                >
                    <span className="text-white font-medium">{d.name}</span>
                    <span className="text-slate-500 text-xs ml-2">{d.mba_name} › {d.module_name}</span>
                </button>
            ))}
        </div>
    )}
</div>
```

- [ ] **Step 5: Adicionar `CheckCircle2` ao import de ícones do MBAList.tsx**

Linha 3 do arquivo:
```typescript
import { Plus, GraduationCap, ChevronRight, Trash2, ArrowLeft, BookOpen, Settings, X } from 'lucide-react';
```

Substituir por:
```typescript
import { Plus, GraduationCap, ChevronRight, Trash2, ArrowLeft, BookOpen, Settings, X, CheckCircle2 } from 'lucide-react';
```

- [ ] **Step 6: Commit**

```bash
git add frontend/src/pages/MBAList.tsx
git commit -m "feat(rf03): add discipline autocomplete with reuse support"
```

---

## Task 6: ERS v1.6

**Files:**
- Create (script temporário): `backend/update_ers.py`
- Output: `/Users/guilhermevalim/Downloads/files/ERS_SistemaCalendario_v1_6.docx`

- [ ] **Step 1: Criar script de atualização do ERS**

```bash
cat > /tmp/update_ers_v16.py << 'PYEOF'
from docx import Document
from docx.oxml.ns import qn
from docx.oxml import OxmlElement
from copy import deepcopy
import datetime

INPUT = "/Users/guilhermevalim/Downloads/files/ERS_SistemaCalendario_v1_5.docx"
OUTPUT = "/Users/guilhermevalim/Downloads/files/ERS_SistemaCalendario_v1_6.docx"

doc = Document(INPUT)

# Atualizar tabela de versão (tabela 0, última linha)
version_table = doc.tables[0]
new_row = deepcopy(version_table.rows[-1])
cells = new_row.cells
cells[0].paragraphs[0].runs[0].text = "1.6"
cells[1].paragraphs[0].runs[0].text = "Guilherme Valim"
cells[2].paragraphs[0].runs[0].text = "18/04/2026"
cells[3].paragraphs[0].runs[0].text = (
    "Implementação completa de RF03 (autocomplete e reutilização de disciplinas), "
    "RF07 (fluxo interativo de resolução de conflitos com suggested_date) e "
    "RF08 (botão explícito de Ajuste Automático por conflito na UI)"
)
version_table._tbl.append(new_row._tr)

# Função para atualizar tabela RF existente
def update_rf_table(table, title, descricao, atores, pre_cond, fluxo, pos_cond, data="18/04/2026", responsavel="Claudio Bonel"):
    rows = table.rows
    # Linha 0: título (merged header) — atualizar primeira célula
    for cell in rows[0].cells:
        for para in cell.paragraphs:
            for run in para.runs:
                run.text = ""
        para = cell.paragraphs[0]
        run = para.add_run(title)
        run.bold = True
    # Linhas 1-6: label + valor
    data_map = {
        1: descricao,
        2: atores,
        3: pre_cond,
        4: fluxo,
        5: pos_cond,
        6: data,
        7: responsavel,
    }
    for row_idx, content in data_map.items():
        if row_idx < len(rows):
            cell = rows[row_idx].cells[1]
            for para in cell.paragraphs:
                for run in para.runs:
                    run.text = ""
            if cell.paragraphs:
                cell.paragraphs[0].add_run(content)

# RF03 — índice de tabela 3 (RF01=1, RF02=2, RF03=3)
rf03 = doc.tables[3]
update_rf_table(
    rf03,
    title="RF03 – Cadastro de Disciplinas",
    descricao=(
        "O sistema permite criar, editar e excluir disciplinas vinculadas a módulos. "
        "Ao adicionar uma disciplina, o administrador pode buscar disciplinas existentes "
        "de outros MBAs via campo de autocomplete (mínimo 2 caracteres) e reutilizá-las, "
        "evitando duplicatas. Se não selecionar uma existente, o sistema cria uma nova disciplina."
    ),
    atores="Administrador de Cronogramas",
    pre_cond="MBA e ao menos um módulo já cadastrados no sistema.",
    fluxo=(
        "1. Administrador acessa a tela de detalhes do MBA e seleciona o módulo desejado.\n"
        "2. Clica em '+ Disciplina'.\n"
        "3. Sistema exibe formulário com campo de nome (autocomplete) e código.\n"
        "4. Ao digitar ≥2 caracteres no campo nome, sistema sugere disciplinas existentes de outros MBAs em dropdown, exibindo nome, módulo de origem e MBA de origem.\n"
        "5a. (Reutilização) Administrador seleciona disciplina existente: nome e código são preenchidos automaticamente; sistema vincula a disciplina ao módulo sem criar novo registro.\n"
        "5b. (Nova) Administrador não seleciona sugestão: preenche nome e código manualmente; sistema cria nova disciplina vinculada ao módulo.\n"
        "6. Administrador clica em 'Salvar'.\n"
        "7. Sistema exibe a disciplina na lista do módulo."
    ),
    pos_cond="Disciplina aparece listada no módulo. Em caso de reutilização, nenhum registro duplicado é criado no banco.",
)

# RF07 — índice de tabela 7
rf07 = doc.tables[7]
update_rf_table(
    rf07,
    title="RF07 – Detecção e Exibição de Conflitos",
    descricao=(
        "Após geração do cronograma, o sistema identifica todas as datas bloqueadas "
        "(feriados ou recessos) e exibe um painel interativo de resolução antes de "
        "confirmar o cronograma. Para cada conflito, o sistema sugere automaticamente "
        "a próxima data válida no mesmo dia da semana (suggested_date)."
    ),
    atores="Administrador de Cronogramas",
    pre_cond="Cronograma gerado (Step 1 preenchido e confirmado). Ao menos uma data conflitante detectada.",
    fluxo=(
        "1. Administrador preenche Step 1 e confirma geração.\n"
        "2. Sistema chama POST /generate-schedule/ e recebe lista de datas válidas e lista de conflitos (date, reason, suggested_date).\n"
        "3. Se houver conflitos, sistema exibe Step 2 – Resolução de Conflitos.\n"
        "4. Para cada conflito, o sistema exibe: data bloqueada, motivo (feriado/recesso), e a data sugerida automaticamente.\n"
        "5. Administrador escolhe ação para cada conflito: Ajuste Automático (aceita suggested_date) ou Ajuste Manual (seleciona data via datepicker).\n"
        "6. Botão 'Aplicar Resoluções' fica habilitado somente quando todos os conflitos têm data resolvida.\n"
        "7. Administrador clica em 'Aplicar Resoluções'.\n"
        "8. Sistema chama POST /schedules/resolve-conflicts/ e avança para Step 3 com o calendário final."
    ),
    pos_cond="Todos os conflitos resolvidos. Cronograma final exibido no Step 3 sem pendências.",
)

# RF08 — índice de tabela 8
rf08 = doc.tables[8]
update_rf_table(
    rf08,
    title="RF08 – Ajuste Automático de Conflitos",
    descricao=(
        "Para cada data bloqueada detectada em RF07, o sistema calcula e sugere automaticamente "
        "a próxima data válida no mesmo dia da semana (suggested_date). O administrador pode "
        "aceitar a sugestão com um clique no botão 'Ajuste Automático', ou optar por data manual. "
        "O endpoint POST /schedules/resolve-conflicts/ incorpora as decisões ao cronograma final."
    ),
    atores="Administrador de Cronogramas",
    pre_cond="Step 2 de Resolução de Conflitos exibido (RF07 executado).",
    fluxo=(
        "1. Sistema exibe card de conflito com botão 'Ajuste Automático' pré-selecionado se suggested_date disponível.\n"
        "2. Administrador clica em 'Ajuste Automático' para confirmar a data sugerida (verde ao selecionar).\n"
        "3. Administrador pode alternar para 'Ajuste Manual' e inserir data diferente via datepicker.\n"
        "4. Ao clicar em 'Aplicar Resoluções', sistema envia resoluções para POST /schedules/resolve-conflicts/.\n"
        "5. Backend gera cronograma base e insere as datas resolvidas, retornando lista final ordenada sem duplicatas."
    ),
    pos_cond="Cronograma final inclui todas as datas resolvidas (automáticas ou manuais) ordenadas cronologicamente.",
)

doc.save(OUTPUT)
print(f"ERS v1.6 salvo em: {OUTPUT}")

# Verificar tabelas
doc2 = Document(OUTPUT)
print(f"Total de tabelas: {len(doc2.tables)}")
for i, t in enumerate(doc2.tables):
    title = t.rows[0].cells[0].text.strip()[:60]
    print(f"  Tabela {i}: {title}")
PYEOF

python3 /tmp/update_ers_v16.py
```

Saída esperada:
```
ERS v1.6 salvo em: /Users/guilhermevalim/Downloads/files/ERS_SistemaCalendario_v1_6.docx
Total de tabelas: 14
  Tabela 0: ...versão...
  Tabela 1: RF01 ...
  Tabela 3: RF03 – Cadastro de Disciplinas
  Tabela 7: RF07 – Detecção e Exibição de Conflitos
  Tabela 8: RF08 – Ajuste Automático de Conflitos
  ...
```

- [ ] **Step 2: Confirmar o arquivo existe**

```bash
ls -lh /Users/guilhermevalim/Downloads/files/ERS_SistemaCalendario_v1_6.docx
```

- [ ] **Step 3: Commit**

```bash
git add docs/
git commit -m "docs: ERS v1.6 generated with RF03, RF07, RF08 fully specified"
```

---

## Task 7: Verificação Final

- [ ] **Step 1: Subir backend e verificar que não há erros de import**

```bash
cd /Users/guilhermevalim/Developer/qualiquanti/prototipoCalendarioAuto/backend
.venv/bin/uvicorn app.main:app --reload --port 8000
```

Esperado: sem erros, `Application startup complete`.

- [ ] **Step 2: Subir frontend**

```bash
cd /Users/guilhermevalim/Developer/qualiquanti/prototipoCalendarioAuto
npm run dev
```

- [ ] **Step 3: Testar fluxo RF07/RF08 manualmente**

1. Acessar `http://localhost:5173` → Gerar Cronograma
2. Preencher Step 1 com data que caia em feriado
3. Confirmar → Step 2 de Resolução deve aparecer com o conflito
4. Clicar "Ajuste Automático" → data sugerida aparece em verde
5. Clicar "Aplicar Resoluções" → Step 3 com calendário final sem conflitos

- [ ] **Step 4: Testar RF03 manualmente**

1. Acessar Gestão de MBAs → abrir um MBA → adicionar disciplina
2. Digitar 2+ caracteres no campo nome → dropdown de sugestões aparece
3. Selecionar disciplina existente → código preenchido automaticamente, badge "Reutilizando" visível
4. Salvar → disciplina aparece na lista do módulo

- [ ] **Step 5: Commit final se necessário**

```bash
git add -A
git commit -m "chore: rf03 rf07 rf08 implementation complete"
```
